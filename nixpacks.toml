[phases.setup]
nixPkgs = ["python311", "bash"]

[phases.install]
cmds = [
    "pip install --upgrade pip", 
    "pip install -r requirements.txt",
    "chmod +x start.sh"
]

[start]
cmd = "./start.sh"

[variables]
PYTHON_VERSION = "3.11"
