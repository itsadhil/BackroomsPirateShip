[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip", 
    "pip install -r requirements.txt",
    "playwright install --with-deps chromium"
]

[start]
cmd = "python bot.py"

[variables]
PYTHON_VERSION = "3.11"
