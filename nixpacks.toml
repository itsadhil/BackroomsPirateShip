[phases.setup]
nixPkgs = ["python311", "bash"]

[phases.install]
cmds = [
    "pip install --upgrade pip", 
    "pip install -r requirements.txt",
    "chmod +x setup.sh",
    "./setup.sh"
]

[start]
cmd = "python bot.py"

[variables]
PYTHON_VERSION = "3.11"
